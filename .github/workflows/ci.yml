name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install requests pytest

      - name: Byte-compile all modules (catches GUI syntax errors CI never imports)
        run: python -m py_compile poe2_pickit_generator.py poe2_pickit_gui.py ui_common.py tab_chance_bases.py tab_craft_bases.py

      - name: Run tests
        run: python -m pytest test_generator.py -v

      - name: Validate [ItemLevel] placement in all generated rules
        run: |
          python - <<'EOF'
          import poe2_pickit_generator as gen

          # Craft bases
          for line in gen.build_craft_base_rules():
              if "[StashItem]" in line and "#" in line:
                  before = line.split("#", 1)[0]
                  assert "[ItemLevel]" not in before, f"[ItemLevel] before # in craft base rule:\n  {line}"

          # Gear bases
          for line in gen.build_base_rules():
              if "[ItemLevel]" in line and "#" in line:
                  before = line.split("#", 1)[0]
                  assert "[ItemLevel]" not in before, f"[ItemLevel] before # in gear base rule:\n  {line}"

          print("All [ItemLevel] placements OK")
          EOF
