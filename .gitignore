# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Build log
build_log.txt

# PyInstaller
build/
dist/
*.spec
# ...except the Qt build spec, which is committed on purpose:
!poe2-pickit-qt/ExileBot2PickitQt.spec

# Generated output (personal, not shared)
pickit_output/
output/

# Local config (contains personal paths/settings)
pickit_gui_config.json
item_states.json
settings.json
base_states.json
history.json

# Backup files
*_backup_*.ipd

# Icon cache (downloaded at runtime, not needed in repo)
icon_cache/
wiki_icon_cache.json

# Price cache (poe.ninja payloads saved at runtime for offline mode)
price_cache/

# Test / scratch files
test_icon.png

# Python virtual environments
.venv/
venv/
env/

# User presets (personal, not shared)
presets/

# Stray generated pickit files
*.ipd

# Windows artifacts
nul
Thumbs.db
desktop.ini

# IDE / editor
.vscode/
.idea/
*.suo
*.user

# Logs and temp
*.log
*.log.*
*.tmp
