# Runtime dependencies for the ExileBot 2 Pickit Generator GUI.
#   pip install -r requirements.txt
# Versions are pinned for reproducible builds.
requests>=2.32.4,<3
Pillow>=11.3
pywebview>=6.2,<7
pystray>=0.19
# Build-only (for producing the .exe): pip install pyinstaller
