PySide6>=6.6
# Network — used by the reused engine (poe2_pickit_generator) and the updater.
requests>=2.28
# Optional — real CPU/RAM in the System Monitor. Without it, values are simulated.
psutil>=5.9
